# Ralph Progress Log — Clicky direct-to-provider (bring-your-own-keys)
Started: 2026-04-11
Branch: ralph/windows-port (continues from the completed Windows port — see archive/progress-windows-port.txt for US-001 through US-017)

## Codebase Patterns

### Product goal (read first)
- Clicky is now being built as a shippable personal tool for end users — not a dev playground. Every user installs the `.exe`, enters their own API keys in a first-run settings window, and runs the app against their own quota on their own machine. There is no shared backend. The `worker/` directory exists for the Mac reference implementation and is unused by the Windows client.
- Keys are encrypted at rest using Windows DPAPI (`ProtectedData.Protect` with `DataProtectionScope.CurrentUser`) and stored at `%APPDATA%\Clicky\secrets.bin`. Non-secret settings live at `%APPDATA%\Clicky\settings.json`.
- UI copy is user-facing, not developer-facing. Prefer "Couldn't reach Anthropic — check your internet connection or your API key in Settings" over "HTTP 401 Unauthorized". Avoid words like "provider", "endpoint", "SSE", "proxy".
- Target vision model for GLM: `glm-4.6v`. GLM-5.1, GLM-5, GLM-5-turbo, GLM-4.5-air are **text-only** and must not be offered — they silently break the `[POINT:x,y:label:screenN]` screen-pointing feature.

### Repo layout and branches
- Monorepo: `mac/` is the Swift/Xcode reference, `windows/` is the C# / .NET 8 / WPF port, `worker/` is legacy shared Cloudflare proxy (used by Mac only).
- Branch: stay on `ralph/windows-port`. The completed Windows port lives there and is NOT on main yet. Creating a new branch from main would lose it.
- Do not edit `mac/` or `worker/` — this PRD is client-side only.

### Architecture after GLM integration (target state)
- `Clicky.Api.ILlmClient` is the common LLM interface: `IAsyncEnumerable<string> SendAsync(history, screens, systemPrompt, userText, ct)`. Two implementations: `AnthropicDirectClient` (→ api.anthropic.com) and `ZaiDirectClient` (→ api.z.ai OpenAI-compat). Both live in `Clicky.Api` and take their API key as a constructor parameter.
- `AssemblyAiStreamingTranscriber` fetches its session token directly from `https://streaming.assemblyai.com/v3/token` with the raw user key in the `Authorization` header (NOT Bearer — AssemblyAI's token endpoint expects the bare key).
- `ElevenLabsTtsClient` POSTs directly to `https://api.elevenlabs.io/v1/text-to-speech/{voiceId}/stream` with `xi-api-key: <userKey>`.
- `Clicky.Companion.SecretsStore` + `SettingsStore` are the single source of truth for user config. `App.xaml.cs` reads them on startup and constructs all four clients with the values.
- `Clicky.Companion.CompanionManager` takes `ILlmClient`, `AssemblyAiStreamingTranscriber`, `ElevenLabsTtsClient` via constructor injection. It must NOT know which concrete LLM implementation it's holding — that knowledge lives in App.xaml.cs's factory.
- `Clicky.App.SettingsWindow` is reachable from the tray right-click menu's `Settings...` item, auto-opens on first launch, and auto-reopens if required keys are missing or invalid at runtime.
- Runtime model switching happens via a tray `Model` submenu that calls `CompanionManager.SwapLlmClientAsync(provider, model)`.

### GLM / OpenAI-compat translation rules
- z.ai OpenAI-compat endpoint: `https://api.z.ai/api/paas/v4/chat/completions`. Auth: `Authorization: Bearer <ZAI_API_KEY>`.
- Request translation (Anthropic → OpenAI chat-completions): `system` prompt → `messages[0] = {role:"system", content: systemPrompt}`; text content blocks → `{type:"text", text}`; image content blocks → `{type:"image_url", image_url:{url:"data:<media_type>;base64,<base64>"}}`; `max_tokens`/`temperature`/`stream:true` preserved.
- Response translation (OpenAI SSE → string deltas): read `data: {...}` lines, extract `choices[0].delta.content`, yield as strings. `[DONE]` sentinel ends the stream. Skip malformed JSON lines, don't throw. Use an incremental UTF-8 decoder so multi-byte chars split across HTTP chunks don't corrupt.
- System prompt (built in CompanionManager from US-010, includes `[POINT:x,y:label:screenN]` instructions) passes through unchanged — GLM-4.6V is prompted in the same English, it just speaks OpenAI over the wire.

### DPAPI gotchas
- DPAPI scope is per-user-account. A backup restored to a different Windows user cannot decrypt. This is correct security behavior — do not add a passphrase fallback.
- DPAPI throws `CryptographicException` on decryption failure — catch it and return null from `SecretsStore.Read`, then let the caller (App.xaml.cs startup) treat it as "missing key" and open SettingsWindow.
- Secrets file format: encrypt the whole JSON dictionary as one blob, not per-entry. Single write on every change.
- `System.Security.Cryptography.ProtectedData` requires `<PackageReference Include="System.Security.Cryptography.ProtectedData" Version="..." />` — it's no longer in the default .NET 8 surface for non-Windows, but Clicky targets `net8.0-windows10.0.19041.0` so it's available; still add the explicit package reference to be safe.

### Test API endpoints (for SettingsWindow Test buttons, US-022)
- **Anthropic**: `POST https://api.anthropic.com/v1/messages` with `{"model":"claude-haiku-4-5","max_tokens":1,"messages":[{"role":"user","content":"hi"}]}`. ~$0.0001 per probe. Headers: `x-api-key`, `anthropic-version: 2023-06-01`.
- **z.ai**: `POST https://api.z.ai/api/paas/v4/chat/completions` with `{"model":"glm-4.5v","max_tokens":1,"messages":[{"role":"user","content":"hi"}]}`. Headers: `Authorization: Bearer <key>`.
- **AssemblyAI**: `GET https://streaming.assemblyai.com/v3/token?expires_in_seconds=60`. Headers: `Authorization: <rawKey>` (no Bearer). Returns a JWT on success, 401 on bad key.
- **ElevenLabs**: `GET https://api.elevenlabs.io/v1/user/subscription`. Headers: `xi-api-key: <key>`. Returns subscription info on success, 401 on bad key. **This is the free auth check — do not run an actual TTS synthesis just to test the key.**

### C# / .NET gotchas carried from Windows port (full details in archive/progress-windows-port.txt)
- All projects target `net8.0-windows10.0.19041.0` (not plain `net8.0-windows`). Changing one project's TFM cascades to all referencing projects.
- `internal` members need `<InternalsVisibleTo Include="Clicky.Tests" />` in the library .csproj for test access.
- `ReadOnlySpan<T>` cannot be used inside `async` iterator methods (`IAsyncEnumerable`) — use `string`/`Substring`.
- Git user config is not set on this Windows host — use `git -c user.name="rickkdev" -c user.email="mail@riccardolamanna.de"` for commits.
- `CompanionManager` owns the hotkey consumer loop and all state transitions. `App.xaml.cs` constructs it and calls `Start()` / `Dispose()` — does not consume hotkey transitions directly.
- Screenshot labels must include `(image dimensions: WxH pixels)` — appended in CompanionManager, not in ScreenCapture.
- Image media_type: sniff PNG vs JPEG by magic bytes. Both Anthropic and z.ai's OpenAI-compat reject mismatched declared types.
- AssemblyAI: reuse a single long-lived `HttpClient` / `ClientWebSocket` factory across sessions. Per-session sockets corrupt the connection pool.
- Push-to-talk hook must be listen-only (always `CallNextHookEx`) — never swallow keys.
- Per-monitor DPI: every WPF window must opt into PerMonitorV2 via `app.manifest`. Overlay coordinate math breaks on mixed-DPI setups otherwise.
- Overlay windows must set `WS_EX_TRANSPARENT | WS_EX_TOOLWINDOW | WS_EX_NOACTIVATE` via `SetWindowLong` in `OnSourceInitialized`.
- WGC Win11+ APIs (IsBorderRequired, IsCursorCaptureEnabled) must be called via reflection when targeting SDK 10.0.19041.
- `LibraryImport` source generators don't handle delegate params with `ref` — use `DllImport` for callbacks like `MonitorEnumProc`.

## Completed Stories

## 2026-04-11 - US-018
- Extracted `ILlmClient` interface from `ClaudeClient` into `Clicky.Api.ILlmClient` with `SendAsync(history, screens, systemPrompt, userText, ct)` signature
- Renamed `ClaudeClient` → `AnthropicDirectClient : ILlmClient` that POSTs directly to `https://api.anthropic.com/v1/messages` with `x-api-key` + `anthropic-version` headers (no worker proxy)
- Constructor: `AnthropicDirectClient(string apiKey, string model = "claude-sonnet-4-6", HttpClient? httpClient = null)`
- `CompanionManager` now takes `ILlmClient` via constructor injection (single constructor, no more worker URL parameter)
- `App.xaml.cs` constructs `AnthropicDirectClient` + `AssemblyAiStreamingTranscriber` + `ElevenLabsTtsClient` explicitly and passes all three into `CompanionManager`
- Temporary API key source: `CLICKY_ANTHROPIC_API_KEY` env var (US-019 will replace with SecretsStore)
- All 7 existing `ClaudeClient` tests pass against `AnthropicDirectClient` (SSE parsing, delta extraction, media type detection)
- New `FakeLlmClientTests` verifies the DI seam: `FakeLlmClient : ILlmClient` yields canned deltas and captures inputs
- Build: 0 warnings. Tests: 113 passed, 0 failed
- Files changed:
  - NEW: `windows/src/Clicky.Api/ILlmClient.cs`
  - NEW: `windows/src/Clicky.Api/AnthropicDirectClient.cs`
  - DELETED: `windows/src/Clicky.Api/ClaudeClient.cs`
  - MODIFIED: `windows/src/Clicky.Companion/CompanionManager.cs`
  - MODIFIED: `windows/src/Clicky.App/App.xaml.cs`
  - MODIFIED: `windows/tests/Clicky.Tests/ClaudeClientTests.cs`
  - NEW: `windows/tests/Clicky.Tests/FakeLlmClientTests.cs`
- **Learnings for future iterations:**
  - `AnthropicDirectClient` sets auth headers on the `HttpClient.DefaultRequestHeaders` in its constructor. When tests pass a custom `HttpClient`, those default headers aren't set — tests that care about headers need to set them on the handler or check differently.
  - The old `ClaudeClient` had two constructors (public for prod, internal for test). The new pattern uses a single public constructor with `ILlmClient` injection — simpler and avoids the dual-constructor smell.
  - `App.xaml.cs` still reads `workerBaseUrl` from `appsettings.json` for audio clients (`AssemblyAiStreamingTranscriber`, `ElevenLabsTtsClient`). US-021 will migrate those to direct API calls too.
---

## 2026-04-11 - US-019
- Created `Clicky.Companion.SecretsStore` — DPAPI-encrypted key-value store at `%APPDATA%\Clicky\secrets.bin`. Whole-file encryption with `DataProtectionScope.CurrentUser`. Methods: `Write`, `Read`, `Delete`, `Exists`. Standardized key constants for all four services.
- Created `Clicky.Companion.SettingsStore` — JSON settings at `%APPDATA%\Clicky\settings.json`. Properties: `LlmProvider`, `LlmModel`, `ElevenLabsVoiceId`, `OnboardingComplete`, `AnalyticsOptOut` with sensible defaults.
- Updated `App.xaml.cs` startup to instantiate both stores, read API key + model from them, and pass into `AnthropicDirectClient` (replacing the env var placeholder from US-018).
- Added registry migration: on first run, copies `HKCU\Software\Clicky\onboarded` → `SettingsStore.OnboardingComplete` and `analyticsOptOut` → `SettingsStore.AnalyticsOptOut`.
- Changed `OnPermissionPollTick` to write `SettingsStore.OnboardingComplete = true` instead of `OnboardingService.MarkOnboardingComplete()`.
- Made `ClickyAnalytics.IsOptedOut()` public (was internal) so migration code in `App.xaml.cs` can access it.
- Added `System.Security.Cryptography.ProtectedData` v8.0.0 package reference to `Clicky.Companion.csproj`.
- Build: 0 warnings. Tests: 136 passed, 0 failed (23 new tests).
- Files changed:
  - NEW: `windows/src/Clicky.Companion/SecretsStore.cs`
  - NEW: `windows/src/Clicky.Companion/SettingsStore.cs`
  - MODIFIED: `windows/src/Clicky.Companion/Clicky.Companion.csproj` (added ProtectedData package)
  - MODIFIED: `windows/src/Clicky.Companion/ClickyAnalytics.cs` (IsOptedOut → public)
  - MODIFIED: `windows/src/Clicky.App/App.xaml.cs` (SecretsStore/SettingsStore init, migration, replaced env var key source)
  - NEW: `windows/tests/Clicky.Tests/SecretsStoreTests.cs` (12 tests)
  - NEW: `windows/tests/Clicky.Tests/SettingsStoreTests.cs` (11 tests)
- **Learnings for future iterations:**
  - `SecretsStore` and `SettingsStore` both have `internal` constructors that accept a file path for testing — use temp dirs in tests to avoid touching real `%APPDATA%`.
  - DPAPI `CryptographicException` is caught and returns null/empty dict — callers treat missing secrets as "open SettingsWindow" (US-023 will implement this).
  - `ClickyAnalytics.IsOptedOut()` is now public — future stories (US-022, US-025) can use it for opt-out checks without registry access.
  - `OnboardingService` is still in the codebase for backward-compat registry reads during migration — it can be removed once all users have migrated (or in US-025 polish).
  - The `workerBaseUrl` read from `appsettings.json` still exists in `App.xaml.cs` for audio clients — US-021 will replace it.
---

## 2026-04-11 - US-020
- Created `Clicky.Api.ZaiDirectClient : ILlmClient` that POSTs to `https://api.z.ai/api/paas/v4/chat/completions` with `Authorization: Bearer <key>` and `Accept: text/event-stream`
- Constructor: `ZaiDirectClient(string apiKey, string model = "glm-4.6v", HttpClient? httpClient = null)`
- Translates Anthropic-shape inputs into OpenAI chat-completions format: system prompt as `messages[0]`, history as alternating user/assistant, images as `image_url` with `data:` URI, labels as text blocks
- Reuses `AnthropicDirectClient.DetectImageMediaType` for PNG/JPEG magic-byte sniffing (internal static, same assembly)
- SSE parsing: reads `data: {...}` lines, extracts `choices[0].delta.content`, yields through `IAsyncEnumerable<string>`. `[DONE]` sentinel ends stream cleanly
- Incremental UTF-8 decoder (`Encoding.UTF8.GetDecoder()`) prevents corruption when multi-byte chars split across HTTP chunk boundaries
- Malformed JSON lines are logged via `Debug.WriteLine` and skipped — one bad line does not kill the stream
- TLS warmup HEAD request to api.z.ai mirrors the AnthropicDirectClient pattern
- Build: 0 warnings. Tests: 155 passed, 0 failed (19 new ZaiDirectClient tests)
- Files changed:
  - NEW: `windows/src/Clicky.Api/ZaiDirectClient.cs`
  - NEW: `windows/tests/Clicky.Tests/ZaiDirectClientTests.cs`
- **Learnings for future iterations:**
  - `AnthropicDirectClient.DetectImageMediaType` is `internal static` — accessible from within `Clicky.Api` and from `Clicky.Tests` (via InternalsVisibleTo). No need to duplicate or refactor to a shared utility.
  - OpenAI SSE format sends an initial chunk with `{"role":"assistant","content":""}` (empty content string) — this is a valid delta that gets yielded. Callers concatenating deltas get correct output since empty string concat is a no-op.
  - The incremental UTF-8 decoder approach (`Encoding.UTF8.GetDecoder()` + `GetChars` with `flush: false`) handles chunk-boundary splits cleanly. This is the correct .NET pattern — avoid `Encoding.UTF8.GetString` on partial byte arrays.
  - `ZaiDirectClient` has its own static TLS warmup flag separate from `AnthropicDirectClient`'s, so both can warm up independently when constructed.
---

## 2026-04-11 - US-021
- Updated `AssemblyAiStreamingTranscriber` to call AssemblyAI's token endpoint directly (`GET https://streaming.assemblyai.com/v3/token?expires_in_seconds=60`) with raw API key in `Authorization` header (not Bearer). Removed the worker proxy `proxyUrl` constructor parameter, replaced with `(string apiKey, HttpClient? httpClient = null)`.
- Updated `ElevenLabsTtsClient` to POST directly to `https://api.elevenlabs.io/v1/text-to-speech/{voiceId}/stream` with `xi-api-key` header. Constructor changed from `(string proxyUrl)` to `(string apiKey, string voiceId, HttpClient? httpClient = null)`. Voice ID is now a constructor parameter read from `SettingsStore.ElevenLabsVoiceId`.
- Updated `App.xaml.cs`: removed `ReadWorkerBaseUrl()` method entirely (no more `appsettings.json` dependency for audio). Both audio clients now constructed with keys from `SecretsStore` and voice ID from `SettingsStore`, matching the pattern established in US-018/US-019.
- All existing US-008 and US-009 tests pass unchanged after the refactor (WebSocket URI builder, keyterm serialization, partial/final turn dispatch, IsPlaying, StopPlayback, Dispose safety, cancellation, TtsRequest serialization).
- New tests: `FetchTokenAsync_UsesDirectEndpointWithRawApiKey` (verifies GET method, URL, and raw Authorization header), `FetchTokenAsync_ThrowsOnBadResponse`, `SpeakAsync_SendsDirectPostWithCorrectUrlAndHeaders` (verifies POST method, URL, xi-api-key header, Accept header), `SpeakAsync_DifferentVoiceId_UsesCorrectUrl` (verifies voice ID parameter threading into URL).
- Build: 0 warnings. Tests: 159 passed, 0 failed (4 new tests).
- Files changed:
  - MODIFIED: `windows/src/Clicky.Api/AssemblyAiStreamingTranscriber.cs` (direct API, new constructor)
  - MODIFIED: `windows/src/Clicky.Api/ElevenLabsTtsClient.cs` (direct API, new constructor)
  - MODIFIED: `windows/src/Clicky.App/App.xaml.cs` (removed ReadWorkerBaseUrl, construct audio clients from SecretsStore/SettingsStore)
  - MODIFIED: `windows/tests/Clicky.Tests/AssemblyAiTranscriberTests.cs` (2 new tests + CapturingHttpMessageHandler)
  - MODIFIED: `windows/tests/Clicky.Tests/ElevenLabsTtsClientTests.cs` (2 new tests + CapturingHttpMessageHandler, updated constructor calls)
- **Learnings for future iterations:**
  - The worker is now completely unused by the Windows client. `appsettings.json` with `WorkerBaseUrl` is no longer read by any code path — it can be removed from the installer in US-025.
  - AssemblyAI's token endpoint uses `GET` (not `POST` like the worker proxy did) and uses the raw API key in `Authorization` (not `Bearer`). This is a common gotcha.
  - Both `AssemblyAiStreamingTranscriber` and `ElevenLabsTtsClient` now accept an optional `HttpClient` for testability, matching the pattern from `AnthropicDirectClient` and `ZaiDirectClient`.
  - The `CapturingHttpMessageHandler` test helper is defined privately in each test class (not shared). If more test files need it, consider extracting to a shared test utility.
---

## 2026-04-11 - US-022
- Created `Clicky.Companion.SettingsViewModel` — MVVM viewmodel for the Settings window. Handles field binding, provider-dependent model list updates, CanSave logic (required keys for selected provider + both audio keys), Test button probes for all four services, Save persistence to SecretsStore + SettingsStore, and a SettingsSaved event.
- Created `Clicky.App.SettingsWindow.xaml` — resizable WPF window with dark theme matching CompanionPanelWindow. Fields: LLM Provider (ComboBox), LLM Model (ComboBox, items change by provider), four API key PasswordBox fields each with Test + "Get key" link buttons, ElevenLabs Voice ID TextBox, Save + Quit buttons, and a help footnote about key encryption.
- Created `Clicky.App.SettingsWindow.xaml.cs` — code-behind wiring PasswordBox changes (WPF PasswordBox doesn't support binding), saved-key placeholder visibility toggling (Change button to clear and re-enter), Test button state display (spinner → green check / red X with tooltip), and browser link openers.
- Updated `TrayIconManager.cs` — added `Settings...` menu item between Open and Quit, with a new `SettingsClicked` event.
- Updated `App.xaml.cs` — subscribes to `SettingsClicked`, opens SettingsWindow with a fresh ViewModel populated from SecretsStore/SettingsStore, wires up SettingsSaved event for future client rebuild (US-023/US-024).
- Test probes: Anthropic → POST /v1/messages with max_tokens:1; z.ai → POST /api/paas/v4/chat/completions with max_tokens:1; AssemblyAI → GET /v3/token (raw Authorization header); ElevenLabs → GET /v1/user/subscription (xi-api-key header).
- User-friendly error copy: 401 → "check your key", 429 → "rate limit", 5xx → "service issues", HttpRequestException → "check your internet", timeout → "try again".
- When reopened after initial save, saved keys show as "••••••••" placeholder with a Change button that clears the field.
- Build: 0 warnings. Tests: 188 passed, 0 failed (29 new SettingsViewModel tests).
- Files changed:
  - NEW: `windows/src/Clicky.Companion/SettingsViewModel.cs`
  - NEW: `windows/src/Clicky.App/SettingsWindow.xaml`
  - NEW: `windows/src/Clicky.App/SettingsWindow.xaml.cs`
  - MODIFIED: `windows/src/Clicky.App/TrayIconManager.cs` (Settings... menu item)
  - MODIFIED: `windows/src/Clicky.App/App.xaml.cs` (SettingsWindow wiring)
  - NEW: `windows/tests/Clicky.Tests/SettingsViewModelTests.cs` (29 tests)
- **Learnings for future iterations:**
  - WPF `PasswordBox` does not support data binding — use `PasswordChanged` event handlers to push the value to the ViewModel.
  - XAML event handlers cannot be `static` methods — the XAML codegen calls them via `this.MethodName()`, so they must be instance methods even if they don't use instance state.
  - The `SettingsViewModel` accepts an optional `HttpClient` for testability, following the same DI pattern used by `AnthropicDirectClient` and `ZaiDirectClient`.
  - `SettingsViewModel.CanSave` depends on the selected provider: Anthropic provider requires Anthropic key, z.ai provider requires z.ai key. Both audio keys (AssemblyAI + ElevenLabs) are always required.
  - Saved keys use a separate `*KeyIsSaved` flag — when true, the PasswordBox is hidden and a read-only placeholder is shown. The Change button clears the flag so the user can enter a new key. Save only writes to SecretsStore when the flag is false (new key entered), preventing overwrite of saved keys with empty strings.
  - Manual browser verification still needed — no browser testing tools configured.
---

## 2026-04-11 - US-023
- Restructured `App.xaml.cs` startup into two phases: pre-settings check and `InitializeApp()`. On startup, checks `HasRequiredKeys()` + `OnboardingComplete`. If either fails, opens SettingsWindow as a modal dialog (`ShowDialog`) before any other initialization.
- First-run mode: SettingsWindow shows "Welcome to Clicky — let's set up your API keys" header and calls `ValidateRequiredFields()` to highlight missing keys with inline red error text.
- After successful save in first-run, sets `OnboardingComplete = true` and calls `InitializeApp()` to continue normal startup (tray icon, overlay, CompanionManager, permissions panel).
- If user closes SettingsWindow via X button during first-run without saving, app exits cleanly via `OnClosing` handler — never leaves the app half-initialized.
- Runtime key error detection: `CompanionManager` catches `HttpRequestException` with 401 status in the response pipeline, sets `CompanionViewModel.LastError` to a friendly message, and raises `OpenSettingsRequested` event which auto-opens SettingsWindow.
- Added `CompanionViewModel.LastError` / `HasError` / `ClearError()` — shown as a dismissable red banner at the top of the CompanionPanelWindow.
- Added `SettingsViewModel.ValidateRequiredFields()` + per-field `*KeyError` properties that highlight missing required keys with inline error text in the XAML. Errors clear automatically when the user types in the field.
- `OnSettingsWindowSaved` now rebuilds all clients: disposes old `CompanionManager`, constructs new `ILlmClient` + audio clients from updated `SecretsStore`/`SettingsStore`, starts a new `CompanionManager`.
- Added `BuildLlmClient()` factory method that reads provider from `SettingsStore` and constructs `AnthropicDirectClient` or `ZaiDirectClient` accordingly — prepares for US-024 runtime model switching.
- Created `NullToVisibilityConverter` for binding error text visibility in XAML.
- Build: 0 warnings. Tests: 203 passed, 0 failed (15 new tests).
- Files changed:
  - MODIFIED: `windows/src/Clicky.Companion/CompanionViewModel.cs` (LastError, HasError, ClearError)
  - MODIFIED: `windows/src/Clicky.Companion/CompanionManager.cs` (OpenSettingsRequested event, key error detection)
  - MODIFIED: `windows/src/Clicky.Companion/SettingsViewModel.cs` (ValidateRequiredFields, per-field errors)
  - MODIFIED: `windows/src/Clicky.App/App.xaml.cs` (first-run flow, InitializeApp, BuildAndStartCompanionManager, client rebuild)
  - MODIFIED: `windows/src/Clicky.App/SettingsWindow.xaml` (inline error text, NullToCollapsed converter)
  - MODIFIED: `windows/src/Clicky.App/SettingsWindow.xaml.cs` (first-run close handling, validate on load)
  - MODIFIED: `windows/src/Clicky.App/CompanionPanelWindow.xaml` (error banner)
  - MODIFIED: `windows/src/Clicky.App/CompanionPanelWindow.xaml.cs` (dismiss error handler)
  - NEW: `windows/src/Clicky.App/NullToVisibilityConverter.cs`
  - NEW: `windows/tests/Clicky.Tests/FirstRunFlowTests.cs` (15 tests)
- **Learnings for future iterations:**
  - `ShowDialog()` blocks the calling thread until the dialog closes — use it for first-run gating where you need to block startup until the user completes setup. `Show()` is non-blocking for the normal tray menu → Settings path.
  - `SettingsWindow.OnClosing` runs before `OnClosed` — use it to trigger `Application.Current.Shutdown()` for the first-run X-close case. The Quit button already calls Shutdown directly.
  - `CompanionManager.OpenSettingsRequested` fires on the dispatcher thread (via `_dispatcher.InvokeAsync`) so the handler can safely open WPF windows.
  - The `BuildAndStartCompanionManager()` pattern (dispose old, construct new) is safer than swapping individual clients because CompanionManager owns CancellationTokenSources and task state. US-024 can add a more surgical `SwapLlmClientAsync` if needed.
  - `HasRequiredKeys` is `internal static` on App so tests can call it via InternalsVisibleTo. It checks the provider-appropriate LLM key + both audio keys.
  - Manual browser verification still needed — no browser testing tools configured.
---

## 2026-04-11 - US-024
- Added `CompanionManager.SwapLlmClientAsync(ILlmClient)` — cancels in-flight response/TTS, waits for current task to finish, swaps the `_llmClient` reference, and resets VoiceState to Idle. Safe to call from any VoiceState.
- Added `CompanionViewModel.LlmModelDisplay` — observable string property for the panel footer showing the active model name.
- Added `ClickyAnalytics.TrackModelSwitched(fromProvider, fromModel, toProvider, toModel)` event, respecting analyticsOptOut.
- Added `ModelMenuEntry` and `ModelSelectedEventArgs` types to `Clicky.App`.
- Updated `TrayIconManager` with a `Model` submenu between `Settings...` and the separator. `UpdateModelMenu()` populates it with radio-style bullet markers for the active model. Disabled items show a tooltip explaining which key is needed.
- Updated `App.xaml.cs`: added `BuildModelMenuEntries()`, `GetModelDisplayName()`, `RefreshModelMenu()`, and `OnModelSelected` handler. Model menu is populated on startup, after settings save, and after model switch. `OnModelSelected` updates SettingsStore, builds a new ILlmClient via the existing `BuildLlmClient()` factory, calls `SwapLlmClientAsync`, updates the ViewModel display, refreshes the menu, and fires analytics.
- Five (provider, model) pairs in the submenu: Claude Sonnet 4.6, Claude Haiku 4.5, Claude Opus 4.6, GLM-4.6V, GLM-4.5V. Items whose required API key is missing are grayed out.
- Build: 0 warnings. Tests: 217 passed, 0 failed (14 new tests).
- Files changed:
  - MODIFIED: `windows/src/Clicky.Companion/CompanionManager.cs` (SwapLlmClientAsync, mutable _llmClient)
  - MODIFIED: `windows/src/Clicky.Companion/CompanionViewModel.cs` (LlmModelDisplay property)
  - MODIFIED: `windows/src/Clicky.Companion/ClickyAnalytics.cs` (TrackModelSwitched)
  - MODIFIED: `windows/src/Clicky.App/TrayIconManager.cs` (Model submenu, ModelMenuEntry, ModelSelectedEventArgs)
  - MODIFIED: `windows/src/Clicky.App/App.xaml.cs` (model menu wiring, OnModelSelected, RefreshModelMenu)
  - NEW: `windows/tests/Clicky.Tests/ModelSwitchingTests.cs` (14 tests)
- **Learnings for future iterations:**
  - `CompanionManager._llmClient` was changed from `readonly` to mutable for `SwapLlmClientAsync`. A `_swapLock` object protects concurrent access, though in practice the swap is serialized through the dispatcher.
  - The `ModelMenuEntry` class lives in `Clicky.App` namespace (not Companion) because it's tightly coupled to the WPF `TrayIconManager`. Tests reference it via `using Clicky.App`.
  - `TrayIconManager.UpdateModelMenu` rebuilds items on every call rather than tracking state — simple and avoids stale item references.
  - `OnModelSelected` is `async void` (event handler pattern) — it awaits `SwapLlmClientAsync` to ensure the swap completes before refreshing the menu.
  - Manual browser verification still needed — no browser testing tools configured.
---

## 2026-04-11 - US-025
- Rewrote `windows/README.md` for end-users: what Clicky does, what keys you need with cost estimates and sign-up links, how to install, how to switch models at runtime, system requirements, building from source. Explicitly notes `worker/` is legacy and unused by Windows.
- Updated root `README.md` to present Windows as a first-class product alongside Mac: added a Platforms table, Windows quick-start section, updated architecture description to cover both platforms, updated project structure to include `windows/`.
- Added three new analytics events to `ClickyAnalytics.cs`: `onboarding_completed` (fired on first successful SettingsSaved), `settings_opened` (tray menu click), `provider_test_run` (properties: service, success, error_category). All respect analyticsOptOut.
- Wired analytics in `App.xaml.cs` (onboarding_completed on first-run save, settings_opened on tray click) and `SettingsViewModel.cs` (provider_test_run on each Test button result).
- Added `ErrorCategory(int statusCode)` and `ErrorCategory(Exception ex)` helpers to `SettingsViewModel` for analytics error categorization (auth_failed, network_error, timeout, etc.).
- Fixed user-facing error copy: replaced `ex.Message` leak in `SettingsViewModel.FriendlyError` default case with generic friendly text. Added friendly error surfacing in `CompanionManager` for recording errors ("check that your microphone is connected"), transcription errors ("check your internet connection or your AssemblyAI key"), non-key HTTP errors ("check your internet connection"), and general pipeline errors ("try again, and if it keeps happening, check Settings").
- Updated installer script (`clicky-installer.iss`): `[Files]` section now excludes `appsettings.json` (no longer needed — keys come from SecretsStore/SettingsStore). Added `CurUninstallStepChanged` Pascal Script procedure that offers to delete `%APPDATA%\Clicky\` on uninstall with an explicit opt-in confirmation dialog (default: No).
- Build: 0 warnings. Tests: 217 passed, 0 failed.
- Files changed:
  - MODIFIED: `README.md` (Windows as first-class platform)
  - MODIFIED: `windows/README.md` (rewritten for end-users)
  - MODIFIED: `windows/scripts/clicky-installer.iss` (exclude appsettings.json, APPDATA cleanup)
  - MODIFIED: `windows/src/Clicky.App/App.xaml.cs` (onboarding_completed + settings_opened analytics)
  - MODIFIED: `windows/src/Clicky.Companion/ClickyAnalytics.cs` (3 new events)
  - MODIFIED: `windows/src/Clicky.Companion/CompanionManager.cs` (friendly error surfacing for all pipeline errors)
  - MODIFIED: `windows/src/Clicky.Companion/SettingsViewModel.cs` (provider_test_run analytics, ErrorCategory helpers, fixed ex.Message leak)
- **Learnings for future iterations:**
  - The `FriendlyError` default case previously leaked raw `ex.Message` to the user — always use generic friendly text for unknown exceptions. Exception details should only go to `Debug.WriteLine`.
  - Inno Setup's `CurUninstallStepChanged` procedure with `usPostUninstall` step runs after files are removed — the right place for optional cleanup of user data directories.
  - Inno Setup `Excludes` flag on `[Files]` entries uses semicolons for multiple patterns (e.g., `Excludes: "appsettings.json;*.pdb"`).
  - Analytics `ErrorCategory` helpers map HTTP status codes and exception types to short stable strings for PostHog aggregation — avoid sending raw status codes or exception type names as event properties.
  - Manual browser verification and full end-to-end testing on a clean Windows 11 VM still needed — no browser testing tools configured.
---

## 2026-04-11 - Microphone + audio output device selectors (user-requested feature)
- User reported wanting to pick a specific mic / audio output instead of always using the Windows default. All PRD stories (US-018 through US-025) had already passed — this is a new shippable feature outside the Ralph loop.
- Implemented:
  - `Clicky.Audio/AudioDevices.cs` — static helper that wraps NAudio's `MMDeviceEnumerator` to enumerate active input (Capture) and output (Render) endpoints and resolve a saved MMDevice ID. Always prepends a "System default" sentinel (empty string ID) so the UI has a safe first entry even on machines where enumeration fails.
  - `Clicky.Audio/MicrophoneCapture.cs` — new `MicrophoneCapture(string? deviceId)` ctor overload. If the ID is empty/missing/invalid we silently fall back to `new WasapiCapture()` rather than throwing — the user's saved preference must never crash push-to-talk.
  - `Clicky.Api/ElevenLabsTtsClient.cs` — new overload `ElevenLabsTtsClient(apiKey, voiceId, string? outputDeviceId, HttpClient?)`. Refactored `_waveOut` field from `WaveOutEvent?` to `IWavePlayer?`. When `outputDeviceId` is set we build a `WasapiOut` bound to that MMDevice; otherwise we keep the existing `WaveOutEvent` path (battle-tested, unchanged for the default case). Tracking field `_wasapiDevice` is needed because NAudio's `WasapiOut` does NOT take ownership of the MMDevice it was constructed with — we have to dispose it ourselves after playback stops.
  - `Clicky.Companion/CompanionManager.cs` — new optional `microphoneDeviceId` ctor parameter; passes it into `new MicrophoneCapture(...)` when a recording session starts. Existing test seams unchanged (default value).
  - `Clicky.Companion/SettingsStore.cs` — new `MicrophoneDeviceId` and `SpeakerDeviceId` string fields (default ""). Persisted as `microphoneDeviceId`/`speakerDeviceId` in `%APPDATA%\Clicky\settings.json`.
  - `Clicky.Companion/SettingsViewModel.cs` — new `MicrophoneDevices`, `SpeakerDevices`, `SelectedMicrophoneDeviceId`, `SelectedSpeakerDeviceId` properties. `LoadFromStores()` falls back to "System default" if the saved device ID no longer matches any enumerated device (handles unplugged USB devices). `Save()` persists both new fields to SettingsStore.
  - `Clicky.App/SettingsWindow.xaml` — new `AUDIO DEVICES` section between ElevenLabs Voice ID and the Save/Quit buttons. Two dark-styled ComboBoxes bound to `MicrophoneDevices`/`SpeakerDevices` with `DisplayMemberPath="FriendlyName"` and `SelectedValuePath="Id"`.
  - `Clicky.App/App.xaml.cs` — `BuildTtsClient()` now passes `_settingsStore.SpeakerDeviceId` into the ElevenLabs ctor; `BuildAndStartCompanionManager()` passes `_settingsStore.MicrophoneDeviceId` into `CompanionManager`. On `SettingsSaved` the manager is rebuilt (existing flow), so a device change from Settings takes effect on the next push-to-talk without a restart.
- New tests:
  - `SettingsStoreTests.MicrophoneAndSpeakerDeviceId_RoundTrip` and `DeviceId_DefaultsToEmptyString`.
  - `SettingsViewModelTests.AudioDeviceLists_AlwaysIncludeSystemDefaultFirst`, `DeviceSelection_DefaultsToSystemDefault`, `Save_PersistsDeviceSelectionsToSettingsStore`, `LoadFromStores_FallsBackToDefaultWhenSavedDeviceIsMissing`.
- Build: 0 warnings. Tests: 223 passed (up from 218).
- Files changed:
  - NEW: `windows/src/Clicky.Audio/AudioDevices.cs`
  - MODIFIED: `windows/src/Clicky.Audio/MicrophoneCapture.cs`
  - MODIFIED: `windows/src/Clicky.Api/ElevenLabsTtsClient.cs`
  - MODIFIED: `windows/src/Clicky.Companion/SettingsStore.cs`
  - MODIFIED: `windows/src/Clicky.Companion/SettingsViewModel.cs`
  - MODIFIED: `windows/src/Clicky.Companion/CompanionManager.cs`
  - MODIFIED: `windows/src/Clicky.App/App.xaml.cs`
  - MODIFIED: `windows/src/Clicky.App/SettingsWindow.xaml`
  - MODIFIED: `windows/tests/Clicky.Tests/SettingsStoreTests.cs`
  - MODIFIED: `windows/tests/Clicky.Tests/SettingsViewModelTests.cs`
- **Learnings for future iterations:**
  - NAudio's `WasapiOut(MMDevice, ...)` does NOT take ownership of the MMDevice — the caller must dispose it after the player is disposed, or you leak COM handles. `WasapiCapture(MMDevice)` behaves the same way. Track the device alongside the player.
  - `MMDeviceEnumerator.GetDevice(id)` throws for unknown IDs rather than returning null. Wrap in try/catch and treat any exception as "device missing" so stale saved preferences don't crash capture/playback.
  - Both `WaveOutEvent` and `WasapiOut` implement `IWavePlayer`, which exposes `Init`, `Play`, `Stop`, `Dispose`, `PlaybackState`, and `PlaybackStopped`. Use `IWavePlayer?` fields when you want to switch between engines without forking the playback code path.
  - The "System default" sentinel uses empty string (`""`) as the ID, not null, because WPF's `SelectedValue` binding does not round-trip null cleanly through a ComboBox's `SelectedValuePath`. Empty string is visible to both the store and the UI.
  - SettingsWindow binds ComboBox items by `Id` via `SelectedValuePath="Id"` + `DisplayMemberPath="FriendlyName"` — this avoids needing custom equality on the `DeviceInfo` record struct.
  - Device IDs are stable MMDevice GUIDs of the form `{0.0.1.00000000}.{guid}`, so they survive app restarts as long as the hardware is still plugged in. A missing device is silently ignored on load (falls back to System default) — do NOT error out.
---

## 2026-04-11 - US-026
- Diagnosed root cause of Quit hang: `CompanionManager.Dispose()` cancelled background CancellationTokenSources but never awaited the fire-and-forget tasks they governed. Three tasks were discarded with `_ = ...`: `ConsumeHotkeyTransitionsAsync` (line 110), `StartRecordingAsync` (line 160), and `StopRecordingAndProcessAsync` (line 173). After a full push-to-talk turn, these tasks hold live HttpClient streams, WebSocket connections, and NAudio WASAPI threads. Under `ShutdownMode="OnExplicitShutdown"`, any non-background foreground thread prevents process exit — the un-awaited tasks kept the process alive indefinitely.
- Fix 1: Converted `CompanionManager` from `IDisposable` to `IAsyncDisposable` (also keeps `IDisposable` as synchronous fallback). `DisposeAsync` cancels all CancellationTokenSources, then awaits all tracked background tasks with a 2-second `Task.WhenAll(...).WaitAsync(TimeSpan)` timeout. If tasks don't finish in 2s, cleanup proceeds anyway.
- Fix 2: Stored all previously fire-and-forget tasks as `Task` fields (`_hookConsumerTask`, `_recordingTask`, `_stopRecordingTask`) so `DisposeAsync` can await them. `_currentResponseTask` was already tracked.
- Fix 3: Added `_disposed` guard to both `Dispose` and `DisposeAsync` for idempotency — prevents `ObjectDisposedException` on double-dispose.
- Fix 4: `App.OnExit` now calls `_companionManager.DisposeAsync().AsTask().Wait(TimeSpan.FromSeconds(2))` instead of synchronous `Dispose()`, giving background tasks time to wind down. Disposal order is deterministic: (1) timers/analytics, (2) CompanionManager (async, 2s timeout), (3) overlay manager, (4) push-to-talk hook, (5) tray icon.
- Fix 5: Added `Environment.Exit(0)` safety net via `Task.Delay(500).ContinueWith(...)` after `base.OnExit(e)` — belt-and-braces to prevent the process lingering if an unforeseen thread survives the dispose chain.
- Fix 6: `BuildAndStartCompanionManager()` (called on settings save to rebuild clients) now uses `DisposeAsync` instead of synchronous `Dispose` for the old manager, preventing the same hang during runtime client rebuild.
- Build: 0 warnings. Tests: 229 passed, 0 failed (6 new shutdown tests).
- Files changed:
  - MODIFIED: `windows/src/Clicky.Companion/CompanionManager.cs` (IAsyncDisposable, tracked tasks, _disposed guard)
  - MODIFIED: `windows/src/Clicky.App/App.xaml.cs` (async dispose in OnExit, ordered disposal, safety net)
  - NEW: `windows/tests/Clicky.Tests/ShutdownTests.cs` (6 tests)
- **Learnings for future iterations:**
  - Never use `_ = SomeAsync()` for tasks that hold network resources or OS handles — always store the Task and await it in Dispose. The fire-and-forget pattern is only safe for truly self-contained work that doesn't block process exit.
  - `Task.WhenAll(...).WaitAsync(TimeSpan)` (.NET 6+) is the clean way to await multiple tasks with a unified timeout. Catches `TimeoutException` on expiry.
  - `IAsyncDisposable` with `DisposeAsync` is the right pattern when cleanup involves awaiting tasks. Keep `IDisposable` as a synchronous fallback for contexts that can't await.
  - `CancellationTokenSource.Cancel()` throws `ObjectDisposedException` if the CTS was already disposed — guard with a `_disposed` flag to make disposal idempotent.
  - Manual verification still needed: launch Clicky, do one push-to-talk turn, click Quit, verify process exits from Task Manager within 1 second. No browser testing tools configured.
---

## 2026-04-11 - US-027
- Implemented version display across three surfaces: tray tooltip, tray context menu, and Settings window.
- Files changed:
  - MODIFIED: `windows/src/Clicky.App/Clicky.App.csproj` (added `<Version>1.0.0</Version>`)
  - NEW: `windows/src/Clicky.App/VersionInfo.cs` (reads `AssemblyInformationalVersionAttribute`, strips `+commithash` suffix)
  - MODIFIED: `windows/src/Clicky.App/TrayIconManager.cs` (tooltip shows `Clicky {version}`, disabled `Version {version}` menu item above Quit)
  - MODIFIED: `windows/src/Clicky.App/SettingsWindow.xaml` (version TextBlock above DPAPI help footnote)
  - MODIFIED: `windows/src/Clicky.App/SettingsWindow.xaml.cs` (sets `VersionText.Text` in constructor)
  - NEW: `windows/tests/Clicky.Tests/VersionInfoTests.cs` (2 tests: non-empty, no commit hash suffix)
- AutoUpdateService does not expose `LatestKnownVersion` — would require WinSparkle callback plumbing. Skipped per story instructions; TODO left for future story if needed.
- Build: 0 warnings. Tests: 231 passed (2 new), 0 failed.
- **Learnings for future iterations:**
  - `<Version>` in the csproj flows into `AssemblyInformationalVersionAttribute` at build time. The SDK may append `+<commithash>` — always strip it for display.
  - `VersionInfo.Current` is a static property suitable for use from both WPF code-behind and WinForms-style tray code without any DI.
  - Manual verification needed: hover tray icon, right-click tray menu, open Settings — all three should show the version string.
---
